# 根系分割自动预测与自主训练项目说明文档

## 1. 项目概述

本项目用于对根系图像进行自动语义分割。模型以每个像素为单位判断其属于背景还是根系，并输出根系区域的二值掩码、概率图和可视化叠加图。

项目当前采用 PaddleSeg 框架和 SegFormer-B2 模型，已经封装为“预测、评估、自训练、模型选择”一体化流程。用户只需要在服务器上启动一次 `auto` 命令，后续步骤由程序自动完成。

自动流程如下：

```text
加载 SegFormer-B2 best_model
        |
        v
预测未标注图像并保存结果
        |
        v
使用人工标注验证集评估模型
        |
        +---- Dice 达标 ----> 保留当前模型，生成最终报告
        |
        +---- Dice 不达标 --> 筛选高质量伪标签
                              |
                              v
                         Student 自训练
                              |
                              v
                         重新评估
                              |
                 Student 提升才允许晋级
```

当前自动流程不会自动修改原始 best model，也不会因为一次训练失败或指标下降而覆盖原模型。

## 2. 当前模型和服务器

| 项目 | 当前配置 |
|---|---|
| 分割模型 | SegFormer-B2 |
| Backbone | MixVisionTransformer_B2 |
| 类别数 | 2 类：背景、根系 |
| 推理框架 | PaddlePaddle + PaddleSeg |
| PaddleSeg 适配 | PaddleSeg 2.8 风格的 `Config` 和 `SegBuilder` |
| 服务器 | `100.102.107.78` |
| SSH 用户 | `xinlei` |
| 服务器项目目录 | `C:\Users\xinlei\root_self_train_project_20260910_1139` |
| GPU | NVIDIA GeForce RTX 4090 D |
| 未标注数据 | `D:\wangxl\auto_train_test_data\Data0828` |
| 当前模型 | `D:\wangxl\RPT\best_model\model.pdparams` |

服务器上的训练、推理和评估均应通过 SSH 执行。本机只负责编辑代码、同步文件和查看少量结果，不作为正式训练设备。

## 3. 项目目录

```text
项目根目录/
├── configs/
│   └── root_segformer_b2.yml       # SegFormer-B2 模型、数据增强、损失和滑窗配置
├── root_self_train/
│   ├── __main__.py                 # 命令行入口和自动编排器
│   ├── backend.py                  # PaddleSeg 模型加载、预测和训练适配层
│   ├── core.py                     # 指标计算和伪标签质量筛选
│   ├── data.py                     # 清单读取、数据校验和哈希检查
│   └── tiling.py                   # 大图滑窗推理
├── dataset/
│   ├── org/                        # 人工标注原图
│   └── label/                      # 人工原始标签
├── data/prepared/
│   ├── train.jsonl                 # 训练图像和标签清单
│   ├── val.jsonl                   # 验证图像和标签清单
│   ├── train.txt                   # PaddleSeg 训练清单
│   ├── val.txt                     # PaddleSeg 验证清单
│   └── unlabeled.jsonl             # 未标注图像清单
├── root_get/                       # 项目内的旧模型和旧配置
├── config.example.yaml              # 配置模板
├── config.local.yaml                # 当前服务器运行配置
├── prepare_local_data.py            # 准备人工数据和未标注数据清单
├── tests/                           # 单元测试
├── README.md                        # 简要使用说明
└── 项目说明文档.md                  # 本文档
```

## 4. 模型说明

模型配置文件为 `configs/root_segformer_b2.yml`，核心设置如下：

```yaml
model:
  type: SegFormer
  backbone:
    type: MixVisionTransformer_B2
  embedding_dim: 768
  num_classes: 2

test_config:
  is_slide: true
  crop_size: [512, 512]
  stride: [400, 400]
```

模型输出两个通道：

- 类别 `0`：背景。
- 类别 `1`：根系。

程序取类别 `1` 的 softmax 概率作为根系概率图。默认阈值为 `0.5`：概率大于等于 `0.5` 的像素被认为是根系。

对于大尺寸图像，程序按照 `512 × 512` 窗口和 `400 × 400` 步长进行滑窗推理，再对重叠区域的 logits 进行融合。

加载 checkpoint 时会严格检查参数名和参数形状。如果模型配置和权重不匹配，程序会直接报错，不会只加载部分参数后继续运行。

## 5. 数据规范

### 5.1 人工标注

人工 mask 必须满足：

- 单通道图像。
- 尺寸与原图完全一致。
- `0` 表示背景。
- `1` 表示根系。
- `255` 表示忽略区域。

如果原始人工标签使用 `255` 表示根系，准备脚本会将其转换为训练所需的 `1`。程序不会自动猜测 `255` 的含义，因此必须确认标签编码。

### 5.2 数据划分

训练集和验证集必须使用不同图像。正式实验建议按照植株、采集批次或实验盒进行划分，而不是简单地随机划分相邻帧。

建议至少保留三类数据：

```text
训练集：用于模型学习
验证集：用于自动判断是否训练、是否晋级
测试集：只在最终报告中使用，不参与训练和模型选择
```

当前项目已有小规模人工数据，训练集 7 张、验证集 2 张，适合流程联调，不适合作为最终论文指标。

### 5.3 未标注图像

当前未标注数据目录为：

```text
D:\wangxl\auto_train_test_data\Data0828
```

程序会读取常见图像格式：`.png`、`.jpg`、`.jpeg`、`.tif`、`.tiff`、`.bmp`。

程序会检查：

- 图像是否损坏。
- 同一数据集合内是否有重复文件。
- 训练、验证和未标注集合之间是否存在完全相同的图像。

## 6. 配置文件说明

当前服务器配置为 `config.local.yaml`：

```yaml
model_config: configs/root_segformer_b2.yml
checkpoint: D:/wangxl/RPT/best_model/model.pdparams
train_manifest: data/prepared/train.jsonl
val_manifest: data/prepared/val.jsonl
unlabeled_dir: D:/wangxl/auto_train_test_data/Data0828
unlabeled_manifest: data/prepared/unlabeled.jsonl
workdir: runs/experiment_rpt_best_001
device: gpu
rounds: 1
iters: 10
batch_size: 1
learning_rate: 0.00001
root_confidence: 0.95
consistency: 0.95
pixel_confidence: 0.9
min_coverage: 0.5
min_root_pixels: 20
max_pseudo_ratio: 1.0
min_delta: 0.001
auto_train: true
auto_train_dice: 0.85
```

重要参数说明：

| 参数 | 作用 |
|---|---|
| `checkpoint` | 初始 Teacher 模型权重 |
| `train_manifest` | 人工训练集清单 |
| `val_manifest` | 人工验证集清单 |
| `unlabeled_dir` | 未标注图像目录 |
| `unlabeled_manifest` | 实际参与本次流程的未标注图像清单 |
| `workdir` | 本次实验输出目录，必须使用新的目录名 |
| `device` | `gpu` 或 `cpu` |
| `rounds` | 最多自训练轮数 |
| `iters` | 每轮训练迭代次数 |
| `batch_size` | 训练 batch 大小 |
| `root_confidence` | 伪标签根系区域平均置信度门槛 |
| `consistency` | 原图和水平翻转预测的一致性门槛 |
| `pixel_confidence` | 单像素赋予确定类别的概率门槛 |
| `min_coverage` | 伪标签中确定像素的最低比例 |
| `max_pseudo_ratio` | 伪标签数量相对人工训练样本的最大比例 |
| `min_delta` | Student 超过 Teacher 的最小 Dice 增量 |
| `auto_train` | 是否允许自动触发自训练 |
| `auto_train_dice` | Dice 低于此值时触发自训练 |

## 7. 自动运行流程

### 7.1 一键运行

在服务器上进入项目目录：

```powershell
cd C:\Users\xinlei\root_self_train_project_20260910_1139
python -m root_self_train auto config.local.yaml
```

`auto` 命令依次执行：

1. 检查人工数据、未标注数据和数据泄漏情况。
2. 加载 SegFormer-B2 和初始 checkpoint。
3. 对未标注数据进行基线预测。
4. 保存 mask、概率图、叠加图和缩略图。
5. 使用验证集计算基线 Dice、IoU、Precision、Recall。
6. 比较基线 Dice 和 `auto_train_dice`。
7. 如果达到目标，直接保留初始模型。
8. 如果没有达到目标，进行伪标签筛选和 Student 微调。
9. 使用同一个固定验证集评估 Student。
10. 只有 Student 的 Dice 大于 Teacher Dice 加 `min_delta` 时才晋级。
11. 使用最终保留 checkpoint 对未标注图像重新预测。
12. 生成最终 JSON、Markdown 报告和自动流程摘要。

### 7.2 单独预测

只想预测而不评估或训练时：

```powershell
python -m root_self_train predict config.local.yaml `
  --input-dir D:/wangxl/auto_train_test_data/Data0828 `
  --output runs/predictions_manual_001
```

### 7.3 单独评估

验证集必须有人工标签：

```powershell
python -m root_self_train evaluate config.local.yaml `
  --output runs/evaluation_manual_001
```

### 7.4 单独启动自训练

```powershell
python -m root_self_train run config.local.yaml
```

建议只有在人工数据已经确认、验证集可靠时使用该命令。

### 7.5 数据和配置检查

该命令不会加载模型，也不会启动训练：

```powershell
python -m root_self_train check config.local.yaml
```

正常输出示例：

```text
Validated: train=7, val=2, unlabeled=133
```

## 8. 预测结果

一次预测输出目录通常包含：

```text
predictions_final/
├── masks/
│   └── 001_mask.png
├── probability/
│   └── 001_prob.png
├── overlays/
│   └── 001_overlay.png
├── thumbnails/
│   └── 001_thumb.jpg
└── prediction_summary.json
```

文件含义：

- `masks`：二值预测结果，根系为 255，背景为 0。
- `probability`：根系概率图，灰度值越亮表示根系概率越高。
- `overlays`：在原图根系区域覆盖红色，用于人工检查。
- `thumbnails`：压缩后的快速浏览图。
- `prediction_summary.json`：记录每张图的路径、根系像素数量、最大概率和根区域平均概率。

## 9. 评估指标

### 9.1 Dice

Dice 衡量预测根系区域和人工根系区域的重叠程度：

```text
Dice = 2TP / (2TP + FP + FN)
```

Dice 越接近 1 越好。根系分割任务建议将 Dice 作为主要指标。

### 9.2 IoU

```text
IoU = TP / (TP + FP + FN)
```

IoU 对预测区域的错误更敏感，适合辅助比较不同模型。

### 9.3 Precision

```text
Precision = TP / (TP + FP)
```

Precision 低表示误把背景或其他结构预测成根系，属于误检较多。

### 9.4 Recall

```text
Recall = TP / (TP + FN)
```

Recall 低表示根系漏检较多。对于细根分割，Recall 通常很重要。

程序采用整个验证集累计 TP、FP、FN 后计算全局指标，而不是简单平均每张图的 Dice，因此结果更适合用于模型选择。

## 10. 伪标签筛选策略

程序不会把所有预测结果都直接拿去训练，而是同时执行以下质量检查：

1. 根系预测像素数量不少于 `min_root_pixels`。
2. 根系区域平均概率不低于 `root_confidence`。
3. 原图预测和水平翻转预测的一致性不低于 `consistency`。
4. 只有双方都高置信的像素才被赋予 0 或 1。
5. 不确定像素标记为 `255`，训练损失中忽略这些像素。
6. 伪标签总量受 `max_pseudo_ratio` 限制。

伪标签中：

- `0`：高置信背景。
- `1`：高置信根系。
- `255`：不确定区域，训练时忽略。

如果没有任何样本通过筛选，流程会输出 `stopped_no_pseudo_labels` 并停止，不会强行训练。

## 11. 自训练和模型晋级机制

每轮自训练始终保留人工训练数据，并将筛选通过的伪标签加入训练清单。

Student 从 Teacher 参数开始微调，但重新初始化优化器，不继承旧优化器状态。训练完成后，Student 在固定验证集上重新评估。

晋级条件为：

```text
Student Dice > Teacher Dice + min_delta
```

如果 Student 指标没有提升：

- 不覆盖 Teacher。
- `best.json` 仍指向原模型。
- 记录失败原因和训练日志。
- 当前流程停止，避免错误模型继续传播。

## 12. 自动流程输出

一次 `auto` 运行的输出目录类似：

```text
runs/experiment_rpt_best_001/
├── config.json
├── predictions_baseline/
├── evaluation_baseline.json
├── evaluation_baseline.md
├── self_train/
│   ├── best.json
│   ├── history.json
│   └── round_01/
│       ├── pseudo/
│       ├── samples.json
│       ├── hard_samples.json
│       ├── train.txt
│       ├── training.log
│       └── result.json
├── predictions_final/
├── evaluation_final.json
├── evaluation_final.md
└── auto_summary.json
```

`auto_summary.json` 是后续 Agent 最重要的接口文件，主要字段包括：

```json
{
  "status": "self_trained_and_selected",
  "target_dice": 0.85,
  "baseline": {
    "dice": 0.8336
  },
  "final": {
    "dice": 0.86
  },
  "checkpoint": "最终保留的模型路径",
  "prediction_dir": "最终预测目录",
  "evaluation": "最终评估报告路径"
}
```

常见状态：

| 状态 | 含义 |
|---|---|
| `accepted_baseline` | 初始模型已达到目标，不需要训练 |
| `self_trained_and_selected` | Student 训练后指标提升，已晋级 |
| `baseline_below_target_training_disabled` | 指标未达标，但配置禁止自训练 |
| `stopped_no_pseudo_labels` | 没有伪标签通过质量筛选 |
| `training_failed` | 训练过程失败，原模型保留 |

## 13. 当前实验结果

使用 `D:\wangxl\RPT\best_model\model.pdparams` 在当前人工验证集上得到过以下结果：

| 指标 | 数值 |
|---|---:|
| Dice | 0.8336 |
| IoU | 0.7147 |
| Precision | 0.8099 |
| Recall | 0.8588 |

这说明 RPT best 权重明显优于之前使用的旧权重。当前验证集只有 2 张图，因此该结果只能作为流程和模型选择参考，不能作为最终泛化性能结论。

## 14. 服务器环境部署

进入远端项目目录后，确认 Python、Paddle 和 PaddleSeg：

```powershell
cd C:\Users\xinlei\root_self_train_project_20260910_1139
python -c "import paddle; print(paddle.__version__); print(paddle.is_compiled_with_cuda())"
python -c "import paddleseg; print('paddleseg ok')"
```

GPU 检查：

```powershell
python -c "import paddle; print(paddle.device.get_device()); print(paddle.utils.run_check())"
```

当前环境曾验证过 Paddle GPU 可用，并使用过 `paddlepaddle-gpu==3.3.0`。

Windows 环境下如果找不到 CUDA DLL，需要确认以下目录已经加入用户 PATH：

```text
Python311\Lib\site-packages\nvidia\cu13\bin\x86_64
Python311\Lib\site-packages\nvidia\cudnn\bin
```

PaddleSeg 的 `CrossEntropyLoss` 曾出现 `float32 * int64` 类型错误，服务器环境中已将系数张量改为跟随有效像素 mask 的 dtype。若重新安装 PaddleSeg，需要重新确认该兼容性问题。

## 15. 常见问题

### 15.1 为什么模型看起来不是 SegFormer-B2？

检查两个文件：

```text
configs/root_segformer_b2.yml
D:\wangxl\RPT\best_model\model.pdparams
```

配置中必须出现：

```yaml
type: SegFormer
backbone:
  type: MixVisionTransformer_B2
```

程序加载 checkpoint 时会严格检查参数名和形状，B0、B1 或其他结构的权重不能直接混用。

### 15.2 为什么预测结果几乎没有根系？

优先检查：

1. 是否误用了 `root_get/model.pdparams` 旧权重。
2. 是否使用了错误的模型 YAML。
3. 是否将类别 0 和类别 1 取反。
4. 预测阈值是否过高。
5. 图像归一化配置是否与训练时一致。
6. 是否把滑窗配置或尺寸恢复逻辑改坏。

### 15.3 为什么自训练没有启动？

检查 `round_01/samples.json`。常见原因是：

- 根系平均置信度低于门槛。
- 原图和翻转预测不一致。
- 预测根系像素太少。
- 有效伪标签覆盖率不足。
- `auto_train` 被设置为 `false`。

### 15.4 为什么不能直接用没有人工标签的数据评估 Dice？

Dice、IoU、Precision 和 Recall 都需要人工真值。没有标签时只能进行质量代理评估，例如：

- 平均根系置信度。
- 翻转一致性。
- 预测根系面积比例。
- 概率图是否集中。
- 人工抽查叠加图。

无标签质量代理不能替代独立人工测试集。

### 15.5 为什么远端推理很慢？

当前大图采用滑窗推理，窗口数量与原图分辨率有关。需要检查：

```powershell
python -c "import paddle; print(paddle.is_compiled_with_cuda()); print(paddle.device.get_device())"
```

如果进程落到 CPU，滑窗推理会明显变慢。不要在本机重新启动大规模任务，应先检查远端 CUDA、Paddle GPU 和 CUDA DLL 环境。

## 16. 组会汇报建议

建议按以下顺序汇报：

1. 问题背景：根系图像数量大，人工逐张分割成本高。
2. 模型基础：采用 SegFormer-B2 完成二分类根系分割。
3. 当前结果：RPT best 模型验证集 Dice 约 0.834，Recall 约 0.859。
4. 自动流程：预测、人工验证集评估、质量筛选、伪标签自训练、Student 晋级。
5. 安全机制：人工验证集作为固定闸门，指标下降不替换模型。
6. 输出结果：mask、概率图、叠加图、逐图指标和自动摘要。
7. 下一步：扩大人工验证集和独立测试集，再接入 Agent 做任务调度和实验决策。

## 17. 后续接入大模型 Agent 的接口设计

当前程序已经具备适合 Agent 调用的命令行接口：

```text
check(config)
predict(config)
evaluate(config)
auto(config)
run(config)
```

后续 Agent 可以负责：

- 读取 `auto_summary.json`。
- 判断是否达到 Dice 目标。
- 汇总多次实验结果。
- 建议需要人工复核的困难样本。
- 根据 `hard_samples.json` 生成待标注清单。
- 调整阈值并启动下一轮实验。
- 生成组会报告或实验日志。

Agent 不应直接绕过以下约束：

- 不覆盖原始 best model。
- 不跳过固定验证集评估。
- 不将所有低置信预测直接作为训练标签。
- 不把测试集用于训练或模型选择。
- 不因为单次指标波动就删除历史实验结果。

推荐的 Agent 工作方式是“调用程序、读取结构化结果、做决策、再次调用程序”，而不是让大模型直接修改权重或手工拼接训练命令。

## 18. 开发和测试

本地运行单元测试：

```powershell
python -m unittest discover -s tests -v
```

当前测试覆盖：

- 忽略像素 `255` 的指标计算。
- Dice、IoU、Precision、Recall 计算。
- 伪标签质量门控。
- 模型晋级边界。
- 多轮自训练编排。
- 滑窗边界和非整除图像尺寸。

修改模型推理、数据清单、伪标签或晋级条件后，应先运行测试，再在服务器执行 `check`，最后使用独立的新 `workdir` 运行真实实验。

