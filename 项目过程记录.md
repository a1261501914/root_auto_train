# 项目过程记录：根系分割自主训练系统

## 一、项目背景

本项目围绕植物根系图像分割任务展开。已有一个基于 PaddleSeg SegFormer-B2 的二分类分割模型，能够对根系图像进行初步预测，但人工标注数据数量较少，直接重新训练容易受数据规模限制。为提高已有模型在新数据上的适应能力，本项目尝试构建一个“伪标签自训练”流程：先用已有模型对未标注图片进行预测，再筛选质量较高的预测结果作为伪标签，最后将人工标注数据和伪标签数据合并，用于后续模型微调。

项目的重点不是重新设计网络结构，而是围绕已有模型搭建一套较完整、可检查、可复现实验流程，包括数据准备、伪标签筛选、训练轮次管理、验证集评估和日志记录。

## 二、项目目标

本项目主要完成以下目标：

1. 复用已有 SegFormer-B2 配置文件和 `.pdparams` 权重，不破坏原始模型文件。
2. 整理人工标注数据，将标签统一转换为训练所需格式。
3. 对未标注图像生成伪标签，并通过置信度、一致性、覆盖率等指标筛选样本。
4. 搭建多轮自训练流程，每一轮从教师模型初始化学生模型并进行有限步数微调。
5. 使用固定验证集计算 Dice、IoU、Precision、Recall 等指标，只有当 Dice 指标提升达到阈值时才晋级保存新模型。
6. 记录训练配置、输入数据、样本筛选结果、训练日志和每轮指标，保证实验过程可追溯。

## 三、项目环境与数据准备

项目使用 Python 编写，主要依赖 PaddlePaddle、PaddleSeg、NumPy、Pillow、PyYAML 等库。代码适配目标为 PaddleSeg 2.8 的 `Config` 和 `SegBuilder` 接口。

本地项目目录中包含以下主要内容：

- `root_self_train/`：自训练主程序，包括伪标签筛选、数据校验、后端预测与训练调度等模块。
- `configs/root_segformer_b2.yml`：在原始 SegFormer-B2 配置基础上整理出的适配配置。
- `root_get/`：保存原始模型配置和权重文件。
- `dataset/`：人工标注图像和标签，共 9 对样本。
- `prepare_local_data.py`：数据准备脚本，用于生成训练、验证和未标注图像清单。
- `tests/`：单元测试，用于检查指标计算、伪标签筛选、滑窗推理和流程编排。
- `README.md`、`SERVER_SETUP.md`：运行说明与服务器迁移说明。

原始人工标签中白色像素值 `255` 表示根系。为了适配训练流程，项目将标签另存为二分类格式：`0` 表示背景，`1` 表示根系，`255` 表示忽略区域。这样做既保留了原始标签，又避免训练时误解标签含义。

在数据划分方面，项目使用固定随机种子 `42`，将 9 张人工标注图片划分为 7 张训练图像和 2 张验证图像。其中 `080`、`605` 两张图片作为验证集。该划分主要用于流程联调；正式实验中还需要扩充验证集，并额外保留独立测试集。

未标注数据目录共扫描到 648 张图像。准备脚本会排除与人工标注数据同名或内容哈希相同的图片，同时排除未标注集合内部的重复图片，最终保留 639 张未标注图片用于后续伪标签生成。

## 四、实现过程

### 1. 项目结构搭建

项目开始时，先明确整体流程：人工数据负责提供可靠监督信号，未标注数据通过教师模型生成伪标签，伪标签经过质量筛选后才参与训练。随后将功能拆分为数据处理、指标计算、预测训练后端、命令行入口和测试用例几个部分。

这样的拆分让项目不依赖单一脚本堆叠逻辑，而是可以分别测试每一部分。例如，伪标签筛选不需要真实 GPU 环境也能用 NumPy 单独验证，训练编排可以用模拟后端测试流程是否正确。

### 2. 数据清单生成

项目使用 JSONL 文件记录训练集、验证集和未标注数据。每一行是一个 JSON 对象，包含图像路径和标签路径，例如：

```json
{"image":"labeled/images/001.png","mask":"labeled/masks/001.png"}
```

这种方式比直接在代码中写死路径更灵活，后续迁移到服务器时只需要重新生成清单，不需要改动主程序。数据准备脚本还会记录输入清单、数据划分和重复图片检查结果，方便追溯实验使用了哪些样本。

### 3. 伪标签生成与筛选

伪标签质量直接影响自训练效果，因此本项目没有简单地把所有预测结果都加入训练，而是设计了多个筛选条件：

- 根区域平均置信度：只统计预测为根系区域的平均概率，避免大量背景像素掩盖根系预测错误。
- 翻转一致性：对原图预测和水平翻转预测进行比较，如果两次预测差异较大，则说明模型对该图不稳定。
- 有效像素覆盖率：只有足够多像素被明确标为背景或根系时，伪标签才有训练价值。
- 根系像素数量：空根预测或根系像素过少的图片不会被选入。

伪标签采用三类像素值：

- `0`：背景；
- `1`：根系；
- `255`：不确定区域，训练时忽略。

这样可以只利用模型高置信区域，而不是强迫模型为所有像素给出标签。

### 4. 自训练流程设计

每一轮训练流程如下：

1. 使用当前教师模型对未标注图片进行预测。
2. 对每张图片进行水平翻转预测，并将结果翻回原坐标。
3. 根据置信度、一致性、覆盖率等条件筛选伪标签。
4. 将通过筛选的伪标签与人工训练数据合并。
5. 从教师模型权重初始化学生模型，重新建立优化器并进行有限轮数训练。
6. 在固定验证集上计算分割指标。
7. 如果 Dice 指标相对上一轮提升超过 `min_delta`，则晋级保存该轮模型；否则停止或保留旧模型。

项目默认最多运行 3 轮，每轮训练 1000 iter。为了服务器联调，当前也准备了一个轻量配置：只运行 1 轮、10 iter、`batch_size=1`，用于先确认推理、训练和评估接口是否连通。

### 5. 滑窗推理支持

由于原始图像分辨率较高，直接整图输入模型可能受到显存限制。本项目根据原配置支持滑窗推理，将图像分块送入模型预测，再把重叠区域的 logits 在 CPU 上累加平均，最后得到整图概率图。

这种方式能够在显存较小的设备上处理大图，也能保持与原训练配置中 `crop_size` 和 `stride` 的设置一致。

### 6. 评估与模型晋级

项目使用验证集计算以下指标：

- Dice；
- IoU；
- Precision；
- Recall。

其中标签值 `255` 的像素会被忽略，不参与 TP、FP、FN 统计。模型晋级以 Dice 为主要依据，只有当新模型 Dice 比旧模型高出至少 `min_delta` 时才认为本轮有效。这样可以避免由于微小波动导致频繁覆盖模型。

训练过程不会覆盖原始权重文件。每次运行会创建新的 `workdir`，保存 `history.json`、`best.json`、`samples.json`、`hard_samples.json` 和训练日志，方便回看每轮发生了什么。

## 五、测试与验证

项目中编写了单元测试，主要覆盖以下内容：

1. 指标计算是否正确处理忽略像素。
2. 伪标签筛选是否能过滤空预测、低一致性预测和非法概率图。
3. Dice 晋级逻辑是否符合预期。
4. 使用模拟后端测试完整两轮流程，包括伪标签生成、训练调度、验证评估和保存结果。
5. 滑窗推理是否能正确覆盖边缘、小图和非法步长情况。

测试命令为：

```bash
python -m unittest discover -s tests -v
```

目前项目已完成基础逻辑测试和流程模拟测试，但还没有在真实 SegFormer 权重和 GPU 环境中完成完整训练。原因是本机 GPU 显存较小，并且当前环境未安装完整 PaddleSeg 训练依赖。后续需要在具备 CUDA 环境的电脑或服务器上按照 `SERVER_SETUP.md` 完成真实联调。

## 六、遇到的问题与解决方法

### 1. 标签含义不统一

原始标签中 `255` 表示根系，但在分割训练中 `255` 常常表示忽略区域。如果直接使用原标签，训练过程可能把根系像素当作忽略像素。解决方法是保留原始标签不动，另存一份训练标签，将根系从 `255` 转换为 `1`。

### 2. 未标注数据可能与人工数据重复

如果未标注图片中包含已经进入训练集或验证集的图像，会造成数据泄漏，使验证结果偏高。项目通过文件名和图片内容哈希进行检查，排除与人工标注数据重复的图片。

### 3. 伪标签质量难以保证

如果直接使用所有预测结果，低质量伪标签会把错误信息带入训练。项目通过根区域置信度、翻转一致性、有效覆盖率和根像素数量等条件进行筛选，同时将不确定区域标为 `255`，训练时忽略。

### 4. 高分辨率图像带来显存压力

原图分辨率较高，直接整图推理可能显存不足。项目实现了滑窗推理，对图像分块预测并合并概率图，使流程更适合大图数据。

### 5. 本地环境无法完成真实训练

本机环境只适合代码开发和基础测试，不适合完整 GPU 训练。项目因此整理了服务器迁移说明，要求在目标 GPU 环境中重新生成数据清单，并先用 1 轮、10 iter 的小配置进行冒烟测试，再扩大训练规模。

## 七、项目成果

本项目最终形成了一个基础版根系分割自训练程序，具备以下成果：

- 完成了数据准备和清单生成流程。
- 完成了人工标签格式转换和重复数据检查。
- 实现了伪标签质量筛选逻辑。
- 实现了多轮自训练调度框架。
- 实现了验证集指标计算和模型晋级机制。
- 实现了滑窗推理支持。
- 编写了单元测试和模拟流程测试。
- 编写了 README 和服务器部署说明，便于后续在 GPU 环境继续训练。

虽然当前还没有完成真实 GPU 上的完整训练结果，但项目已经具备可迁移、可检查、可继续实验的基础结构。

## 八、总结与反思

通过本项目，我对语义分割任务中的数据准备、模型训练和实验管理有了更系统的认识。相比单纯调用模型训练，本项目更强调训练流程的可靠性：数据是否泄漏、标签含义是否一致、伪标签是否可信、指标是否能真实反映模型变化，这些都会直接影响最终结果。

本项目也让我认识到，深度学习项目不只是模型结构本身，数据流程和实验记录同样重要。尤其是在标注数据较少的场景下，伪标签自训练可以扩展训练数据，但如果缺少质量筛选，也可能放大模型错误。因此，后续工作应重点放在真实 GPU 环境训练、扩充验证集、补充独立测试集，并根据实际预测效果调整伪标签筛选阈值。

总体来说，本项目完成了根系分割自训练流程的基础搭建，为后续正式训练和实验优化提供了可复用的代码框架。
