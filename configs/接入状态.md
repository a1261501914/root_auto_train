# 本地模型与数据接入

已找到 `root_get/model.pdparams`（109,431,423 字节）和原始 SegFormer-B2 配置。
`root_segformer_b2.yml` 是适配副本，保留原网络、训练增强、交叉熵损失以及 512×512、步长 400×400 的滑窗参数。
副本中的清单路径由 `prepare_local_data.py` 生成。原始配置与权重未修改。

`input_inventory.json` 记录了 9 对图像和标签，图像均为 5472×3648。标签像素为 0/255。

数据准备规则：

- 用户已确认白色 255 表示根系，另存 0/1 标签至 `data/prepared/masks`，原标签不修改。
- 用户已确认 9 张都未参与原权重训练。固定随机种子 42，7 张训练；080、605 两张验证，仅作联调划分。
- 未标注目录为 `G:/2025夏季欧阳老师大根盒水稻实验/2025_big_rice_org/Data1011`，共 648 张。
- 准备脚本排除与已标注文件同名或同哈希的图像，以及未标注集合内部的重复文件，输出 JSONL 清单，无需复制 G 盘图片。
- 目录内全部未标注清单为 `data/prepared/unlabeled.jsonl`；冒烟清单只取前 8 张。
- 完整扫描结果：648 张中排除 9 张已标注同名/同内容图片，保留 639 张；详见 `data/prepared/split_report.json`。

环境检查：

- 本机 GPU 为 GeForce GT 730，2 GB 显存，驱动 388.31。
- `C:/Users/Administrator/anaconda3/envs/rpt_env/python.exe` 为 Python 3.10.18，已安装 CPU 版 Paddle 3.1.0，没有 PaddleSeg。
- 已用该环境成功反序列化权重，包含 347 个参数项；输出层权重形状为 `[2,768,1,1]`，与配置的二分类输出一致。尚未完成所有参数与实际模型的匹配检查。
- `config.local.yaml` 为 1 轮、10 iter、batch_size=1 的 CPU 联调配置；运行前需要安装兼容的 PaddleSeg。用户已指定在另一台 GPU 电脑或服务器训练，按 `SERVER_SETUP.md` 在目标机器重新生成 GPU 配置。

已新增 CPU 累计 logits 的滑窗推理，单个图块送入模型，重叠区取平均后计算概率。
边缘覆盖、小图和非法步长测试通过；尚未使用真实权重验证预测及训练。
